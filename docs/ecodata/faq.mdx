---
id: faq
title: Câu hỏi thường gặp (FAQ)
description: Giải đáp nhanh các câu hỏi thường gặp về EcoData — nguồn dữ liệu, định dạng xuất, kinh tế lượng, metadata và giá.
---

import Head from '@docusaurus/Head';

export const faqs = [
  {
    q: 'EcoData là gì và dành cho ai?',
    a: 'EcoData là nền tảng dữ liệu kinh tế – tài chính phục vụ nghiên cứu và công bố. Nền tảng tổng hợp, làm sạch và chuẩn hóa dữ liệu vĩ mô quốc tế cùng dữ liệu chính thức của Việt Nam, sẵn sàng cho phân tích và xuất bản. Đối tượng chính là giảng viên, nhà nghiên cứu, học viên cao học và chuyên gia phân tích kinh tế.',
  },
  {
    q: 'EcoData tổng hợp dữ liệu từ những nguồn nào?',
    a: 'Dữ liệu vĩ mô quốc tế đến từ World Bank, IMF, ADB, UN, FRED, OECD, ILO, Eurostat, FAO, WTO và UNCTAD. Dữ liệu Việt Nam đến từ Tổng cục Thống kê (GSO), Tổng cục Hải quan, Bộ Tài chính, các khảo sát quản trị (PCI, PAPI, PAR, SIPAS, ICT) và các bộ vi mô (VHLSS, VARHS, VES).',
  },
  {
    q: 'Dữ liệu Việt Nam trên EcoData gồm những gì?',
    a: 'Gồm năm nhóm chính: thống kê chính thức GSO (Niên giám thống kê, báo cáo kinh tế – xã hội, số liệu cấp quốc gia và cấp tỉnh); dữ liệu xuất nhập khẩu của Hải quan theo mặt hàng, quốc gia đối tác, tỉnh và phương thức vận tải; các khảo sát quản trị cấp tỉnh (PCI, PAPI, PAR, SIPAS, ICT); dữ liệu vi mô hộ gia đình (VHLSS, VARHS, VES); và dữ liệu chứng khoán (giá, báo cáo tài chính, sự kiện doanh nghiệp).',
  },
  {
    q: 'EcoData khác gì so với tải trực tiếp từ World Bank, IMF hay GSO?',
    a: 'EcoData hợp nhất nhiều nguồn rời rạc về một định dạng panel/time-series thống nhất, đã được làm sạch, chuẩn hóa tên chỉ tiêu (short code) và kèm metadata. Người dùng tiết kiệm thời gian thu thập – đối chiếu – chuẩn hóa, và có dữ liệu sẵn sàng đưa thẳng vào mô hình kinh tế lượng thay vì xử lý thủ công từng nguồn.',
  },
  {
    q: 'Có thể xuất dữ liệu ở những định dạng nào và có giới hạn gì?',
    a: 'Dữ liệu có thể xuất ra CSV, Excel (XLSX) và JSON, kèm metadata. Mỗi gói thuê bao có hạn mức số lượt xuất mỗi ngày khác nhau; việc xem trước (preview) và duyệt danh mục là miễn phí. Xem trang "Xuất dữ liệu" để biết chi tiết định dạng và hạn mức theo từng gói.',
  },
  {
    q: 'EcoData có hỗ trợ phân tích kinh tế lượng và xuất mã Stata/R/Python không?',
    a: 'Có. Công cụ Kinh tế lượng tích hợp cho phép chuẩn bị dữ liệu, ước lượng và kiểm định nhiều mô hình (OLS, WLS, GLS, panel, VAR, ARIMA, Logit, Probit và nhiều ước lượng khác), trình bày bảng kết quả theo chuẩn trích dẫn, và xuất bộ mã đầy đủ chạy trực tiếp trên Stata, R hoặc Python để tái lập toàn bộ quy trình.',
  },
  {
    q: 'Dữ liệu có kèm metadata và trích dẫn để tái lập nghiên cứu không?',
    a: 'Có. Mỗi bộ dữ liệu xuất ra đi kèm metadata (định nghĩa, đơn vị đo, tần suất, nguồn gốc) và short code chuẩn hóa, giúp người dùng trích dẫn nguồn và tái lập kết quả nghiên cứu một cách minh bạch.',
  },
  {
    q: 'EcoData có giao diện tiếng Anh không? Có cần trả phí không?',
    a: 'EcoData hỗ trợ song ngữ Tiếng Việt và English. Duyệt danh mục và xem trước dữ liệu là miễn phí; để xuất đầy đủ dữ liệu và dùng một số tính năng nâng cao (Survey datasets, AI Chat) cần đăng ký gói thuê bao theo tháng, 6 tháng, năm hoặc trọn đời.',
  },
];

export const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  inLanguage: 'vi',
  mainEntity: faqs.map((f) => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a },
  })),
};

<Head>
  <script type="application/ld+json">{JSON.stringify(faqSchema)}</script>
</Head>

# Câu hỏi thường gặp (FAQ)

Những câu hỏi thường gặp nhất về EcoData — nền tảng dữ liệu kinh tế – tài chính cho nghiên cứu và công bố. Mỗi câu trả lời được viết ngắn gọn, có định nghĩa và bối cảnh.

{faqs.map((f) => (
  <div key={f.q} style={{ marginBottom: '1.5rem' }}>
    <h2 style={{ fontSize: '1.15rem' }}>{f.q}</h2>
    <p>{f.a}</p>
  </div>
))}

## Tìm hiểu thêm

- [Tổng quan EcoData](/ecodata/overview)
- [Bắt đầu: Login → Filter → Preview → Export](/ecodata/bat-dau/login-filter-preview-export)
- [Tổng hợp nhóm indicators](/ecodata/indicators/tong-hop-indicators)
- [Xuất dữ liệu và giới hạn gói](/ecodata/xuat-du-lieu/preview-export)
